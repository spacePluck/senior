# AI 기반 매장-시니어 매칭 시스템 기획서

> **"시니어의 상황과 선호를 이해하고, 가장 적합한 매장을 추천"**

---

## 📋 개요

### 배경

**시니어의 고민:**
- "이발소가 너무 많아. 어디가 좋을까?"
- "무릎이 안 좋은데, 계단 없는 곳은 어디지?"
- "귀가 잘 안 들리는데, 큰 소리로 말해주는 곳 있나?"
- "우리 딸이 자주 가는 병원은 어디였더라..."

**기존 검색의 문제점:**
1. 단순 거리순, 별점순 정렬 (시니어 개별 상황 무시)
2. 리뷰가 너무 많아서 읽기 어려움
3. 본인에게 맞는지 판단 어려움

### 우리의 해결책

**AI 기반 개인화 추천:**
1. 시니어의 **신체 상태** 고려 (휠체어, 무릎 통증, 청각 등)
2. **과거 이용 이력** 학습 (선호 매장, 가격대, 서비스 스타일)
3. **가족 선호** 반영 (가족이 자주 보내는 매장)
4. **실시간 상황** 반영 (날씨, 시간, 위치)

---

## 🎯 핵심 시나리오

### Case 1: 김영수 어르신 (75세, 무릎 관절염)

**프로필:**
```
이름: 김영수
나이: 75세
거주지: 서울 강동구 천호동
건강 상태:
  - 무릎 관절염 (계단 오르기 어려움)
  - 청각 약함 (보청기 착용)
  - 시력 정상 (안경 착용)
이동 수단: 도보 (버스 가능, 지하철 어려움)

과거 이용 이력:
  - 명성이발관 (5회 방문, 평점 5.0)
  - 할매 순대국 (3회 방문, 평점 4.5)
  - 천호 정형외과 (2회 방문, 평점 5.0)

선호 사항:
  - 가격대: 저렴 (1-2만원)
  - 매장 분위기: 조용함
  - 사장님 스타일: 친절하고 말 많은 편
```

#### 시나리오: 이발소 찾기

**시니어 앱에서 "이발" 검색:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 이발

🎯 영수님께 딱 맞는 곳이에요!

━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 1순위 추천

📍 명성이발관 ⭐⭐⭐⭐⭐
   거리: 200m (도보 3분)
   가격: 15,000원

✅ 영수님이 5번 방문한 단골집
✅ 1층이라 계단 없음
✅ 사장님이 큰 소리로 말해줘요
✅ 조용하고 편안한 분위기

[예약하기]

━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 2순위 추천

📍 친절이발소 ⭐⭐⭐⭐
   거리: 450m (도보 7분)
   가격: 12,000원

✅ 1층, 경사로 설치
✅ 평점 4.8 (후기 120개)
⚠️ 비슷한 분들이 "친절해요" 후기 많음

[자세히 보기]

━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 3순위

📍 신촌이발관 ⭐⭐⭐⭐
   거리: 800m (버스 10분)
   가격: 10,000원

✅ 저렴한 가격
⚠️ 2층이라 계단 있음 (비추천)

[자세히 보기]
━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔄 [다른 기준으로 찾기]
   거리순 | 가격순 | 평점순
━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**AI 추천 이유:**
1. **1순위 (명성이발관)**: 과거 5회 방문 + 평점 5.0 → 가장 만족한 곳
2. **2순위 (친절이발소)**: 1층 + 경사로 (무릎 배려) + 평점 우수
3. **3순위 (신촌이발관)**: 가격 저렴하지만 2층이라 하위 추천

---

### Case 2: 박순자 어르신 (68세, 치매 초기 남편 케어)

**프로필:**
```
이름: 박순자
나이: 68세
거주지: 부산 해운대
가족 상황: 남편 (72세, 치매 초기)
건강 상태: 당뇨, 허리 디스크
이동 수단: 자가용 (남편과 함께)

관심 서비스:
  - 치매 관련 의료 서비스
  - 청소 대행 (허리 부담 줄이기)
  - 식사 배달 (저염식)

가족 정보:
  - 딸 (서울 거주)이 자주 청소 대행 예약해줌
```

#### 시나리오: 청소 업체 찾기

**시니어 앱에서 "청소" 검색:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 청소

🎯 순자님께 딱 맞는 곳이에요!

━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 1순위 추천

🏠 깨끗한 청소 ⭐⭐⭐⭐⭐
   가격: 80,000원 (3시간)

✅ 딸님이 3번 예약해준 곳
✅ 치매 어르신 케어 경험 많음
✅ "조용하고 꼼꼼해요" 후기
✅ 청소 전/후 사진 자동 전송

[예약하기]

━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 2순위 추천

🏠 해운대 홈케어 ⭐⭐⭐⭐
   가격: 70,000원 (2.5시간)

✅ 시니어 전문 청소
✅ 평점 4.7
⚠️ 치매 케어 경험 없음

[자세히 보기]
━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 딸님께서 지난달에 예약해주신
   "깨끗한 청소"를 다시 부를까요?

[같은 곳 예약]
━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**AI 추천 이유:**
1. **가족 이용 이력 우선**: 딸이 3번 예약 → 가족이 신뢰하는 곳
2. **특수 케어 경험**: 치매 환자 대응 가능
3. **재예약 편의**: "같은 곳 예약" 버튼으로 한 번에 처리

---

## 🧠 AI 추천 알고리즘 구조

### 입력 데이터 (Input)

#### 1. 시니어 프로필 데이터
```json
{
  "user_id": "senior_12345",
  "age": 75,
  "location": "서울 강동구 천호동",
  "health": {
    "mobility": "무릎 관절염 (계단 어려움)",
    "hearing": "청각 약함 (보청기)",
    "vision": "정상 (안경)"
  },
  "preferences": {
    "price_range": "저렴 (1-2만원)",
    "atmosphere": "조용함",
    "service_style": "친절하고 말 많은 편"
  },
  "past_visits": [
    {
      "store": "명성이발관",
      "count": 5,
      "rating": 5.0,
      "last_visit": "2025-01-10"
    }
  ]
}
```

#### 2. 매장 정보 데이터
```json
{
  "store_id": "store_67890",
  "name": "명성이발관",
  "category": "이발",
  "location": "서울 강동구 천호대로 123",
  "distance_from_user": 200,  // 미터
  "price": 15000,
  "accessibility": {
    "floor": 1,  // 1층
    "stairs": false,  // 계단 없음
    "ramp": false,
    "restroom_handrail": true  // 화장실 손잡이
  },
  "features": [
    "큰 소리로 설명",
    "조용한 분위기",
    "예약 우선"
  ],
  "ratings": {
    "average": 4.8,
    "count": 87,
    "senior_rating": 4.9  // 시니어 평점
  }
}
```

#### 3. 실시간 컨텍스트
```json
{
  "current_time": "2025-01-20 10:30",
  "day_of_week": "월요일",
  "weather": "맑음, 5도",
  "user_location": "37.5401, 127.1285"
}
```

---

### AI 스코어링 모델

#### 가중치 (Weight) 설정
```python
WEIGHTS = {
    "past_visit": 0.30,        # 과거 방문 이력 (30%)
    "accessibility": 0.25,     # 접근성 (25%)
    "distance": 0.20,          # 거리 (20%)
    "rating": 0.15,            # 평점 (15%)
    "price": 0.10              # 가격 (10%)
}
```

#### 스코어 계산 로직
```python
def calculate_match_score(user, store, context):
    score = 0

    # 1. 과거 방문 이력 (30%)
    past_visit_count = get_past_visit_count(user, store)
    if past_visit_count > 0:
        score += 30 * min(past_visit_count / 5, 1.0)

    # 2. 접근성 (25%)
    accessibility_score = 0
    if user.health.mobility == "무릎 관절염":
        if store.accessibility.floor == 1:
            accessibility_score += 15
        if not store.accessibility.stairs:
            accessibility_score += 10
    score += accessibility_score

    # 3. 거리 (20%)
    distance = store.distance_from_user
    if distance < 300:
        score += 20
    elif distance < 500:
        score += 15
    elif distance < 1000:
        score += 10
    else:
        score += 5

    # 4. 평점 (15%)
    rating = store.ratings.senior_rating or store.ratings.average
    score += (rating / 5.0) * 15

    # 5. 가격 (10%)
    price_match = match_price_preference(user.preferences.price_range, store.price)
    score += price_match * 10

    return score
```

---

## 🔍 개인화 추천 요소

### 1. 건강 상태 기반 필터링

#### 이동 장애
```
휠체어 사용자:
  ✅ 1층 매장만
  ✅ 경사로 필수
  ✅ 넓은 출입구 (80cm 이상)
  ✅ 화장실 휠체어 접근 가능

무릎 관절염:
  ✅ 1-2층 매장 (엘리베이터 있으면 3층까지 가능)
  ✅ 계단 없는 곳 우선
  ⚠️ 계단 있으면 경고 표시

거동 불편 (지팡이):
  ✅ 손잡이 설치
  ✅ 미끄럽지 않은 바닥
```

#### 청각 장애
```
청각 약함 (보청기):
  ✅ "큰 소리로 설명" 태그 매장 우선
  ✅ 조용한 환경 (배경 소음 적음)
  ⚠️ "시끄러움" 후기 있으면 하위 추천
```

#### 시각 장애
```
시력 약함:
  ✅ 밝은 조명
  ✅ 큰 글씨 메뉴판
  ⚠️ "어두움" 후기 있으면 하위 추천
```

---

### 2. 과거 이용 이력 학습

#### 패턴 분석
```python
# 시니어의 선호 패턴 추출
def analyze_user_pattern(user_id):
    visits = get_user_visits(user_id)

    # 자주 가는 매장 특성
    favorite_stores = visits.filter(rating >= 4.5)

    patterns = {
        "preferred_price_range": extract_price_range(favorite_stores),
        "preferred_distance": average_distance(favorite_stores),
        "preferred_atmosphere": extract_keywords(favorite_stores.reviews),
        "preferred_service_style": extract_service_style(favorite_stores)
    }

    return patterns
```

**예시:**
```
김영수 어르신의 패턴:
  - 가격대: 평균 15,000원 (1-2만원대)
  - 거리: 평균 300m (도보 5분 이내)
  - 분위기: "조용함", "편안함" 키워드 많음
  - 서비스: "친절함", "설명 자세함" 선호
```

---

### 3. 가족 선호 반영

#### 가족이 자주 예약해준 매장
```python
# 가족의 예약 이력 확인
def get_family_preferences(senior_id):
    family_members = get_linked_family(senior_id)
    family_bookings = []

    for family in family_members:
        bookings = get_bookings_by(family, for_senior=senior_id)
        family_bookings.extend(bookings)

    # 가족이 3회 이상 예약한 매장은 "가족 신뢰" 배지
    trusted_stores = [
        store for store in family_bookings
        if store.count >= 3
    ]

    return trusted_stores
```

**화면 표시:**
```
🏠 깨끗한 청소 ⭐⭐⭐⭐⭐

✅ 딸님이 3번 예약해준 곳
💙 가족이 신뢰하는 매장
```

---

### 4. 실시간 상황 반영

#### 날씨 기반
```python
def adjust_by_weather(stores, weather):
    if weather.condition == "비" or weather.condition == "눈":
        # 실내 매장 우선
        # 거리 가중치 2배 (가까운 곳 우선)
        for store in stores:
            if store.distance > 500:
                store.score *= 0.7  # 먼 곳은 점수 하락
    return stores
```

**예시:**
```
날씨: 비 오는 날
  → "200m 거리의 명성이발관"을 "800m 거리의 신촌이발관"보다 우선 추천
```

#### 시간대 기반
```python
def adjust_by_time(stores, current_time):
    hour = current_time.hour

    # 점심시간 (12-13시) → 음식점 대기 많음
    if 12 <= hour <= 13:
        for store in stores:
            if store.category == "음식점":
                store.waiting_time += 30  # 대기 시간 예상

    return stores
```

---

## 💬 AI 추천 설명 (Explainability)

### 왜 이 매장을 추천했는지 명확히 설명

**좋은 예시:**
```
✅ 영수님이 5번 방문한 단골집
✅ 1층이라 계단 없음
✅ 사장님이 큰 소리로 말해줘요
```

**나쁜 예시:**
```
❌ "추천 매장입니다" (이유 없음)
❌ "인기 매장" (막연함)
```

### 주의 사항 표시
```
⚠️ 2층이라 계단 있음 (비추천)
⚠️ "시끄럽다"는 후기가 있어요
⚠️ 평소보다 30분 더 걸릴 수 있어요 (날씨)
```

---

## 🗂️ 매장 태그 시스템

### 시니어 친화 태그

#### 접근성 태그
- `#1층` `#경사로` `#엘리베이터` `#넓은출입구`
- `#화장실손잡이` `#미끄럼방지`

#### 서비스 태그
- `#큰소리설명` `#천천히설명` `#친절함`
- `#조용한분위기` `#편안함` `#예약우선`

#### 특수 케어 태그
- `#치매케어` `#휠체어가능` `#보청기친화`
- `#저염식가능` `#당뇨식가능`

#### 가격 태그
- `#저렴` `#합리적` `#고급`

#### 거리 태그 (자동 생성)
- `#도보3분` `#도보10분` `#버스20분`

---

## 📊 매칭 품질 모니터링

### 추천 정확도 측정

#### 지표
```
클릭률 (CTR):
  추천 1순위 클릭 / 전체 검색

예약률:
  추천 매장 예약 / 추천 매장 클릭

재방문율:
  동일 매장 2회 이상 방문 / 전체 방문

만족도:
  추천 매장 평균 평점 / 전체 매장 평균 평점
```

#### 목표 KPI
```
1순위 클릭률: 60% 이상
추천 매장 예약률: 40% 이상
재방문율: 30% 이상
추천 매장 평균 평점: 4.5 이상
```

---

## 🔧 기술 구현

### 추천 시스템 아키텍처

```
[시니어 앱]
    ↓ 검색 요청 (예: "이발")
[API 서버]
    ↓ 사용자 프로필 + 컨텍스트
[AI 추천 엔진]
    ├─ 과거 이력 분석
    ├─ 건강 상태 필터링
    ├─ 거리 계산
    ├─ 실시간 조정 (날씨 등)
    └─ 스코어링
    ↓ 정렬된 매장 리스트
[API 서버]
    ↓ 추천 이유 생성
[시니어 앱]
```

### AI 모델
```python
# 협업 필터링 + 콘텐츠 기반 하이브리드

import numpy as np
from sklearn.metrics.pairwise import cosine_similarity

class SeniorStoreRecommender:
    def __init__(self):
        self.user_profiles = {}
        self.store_features = {}

    def recommend(self, user_id, category, location):
        # 1. 후보 매장 필터링
        candidate_stores = self.filter_stores(
            category=category,
            location=location,
            user_constraints=self.get_user_constraints(user_id)
        )

        # 2. 각 매장 스코어 계산
        scores = []
        for store in candidate_stores:
            score = self.calculate_match_score(user_id, store)
            scores.append((store, score))

        # 3. 정렬 및 반환
        sorted_stores = sorted(scores, key=lambda x: x[1], reverse=True)
        return sorted_stores[:5]  # 상위 5개

    def calculate_match_score(self, user_id, store):
        user = self.user_profiles[user_id]

        # 과거 방문 가중치
        past_visit_score = self.get_past_visit_score(user_id, store.id)

        # 건강 상태 매칭
        accessibility_score = self.match_accessibility(user.health, store.accessibility)

        # 거리 점수
        distance_score = self.calculate_distance_score(user.location, store.location)

        # 평점 점수
        rating_score = store.ratings.senior_rating / 5.0

        # 가격 매칭
        price_score = self.match_price(user.price_preference, store.price)

        # 가중 합산
        total_score = (
            past_visit_score * 0.30 +
            accessibility_score * 0.25 +
            distance_score * 0.20 +
            rating_score * 0.15 +
            price_score * 0.10
        )

        return total_score
```

---

## 🎨 UI/UX 설계

### 추천 결과 화면

**큰 글씨, 명확한 아이콘:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 이발

🎯 영수님께 딱 맞는 곳이에요!

━━━━━━━━━━━━━━━━━━━━━━━━━━━
[큰 카드 형태]

⭐⭐⭐⭐⭐ (1순위)

📍 명성이발관
💰 15,000원
📏 200m (도보 3분)

✅ 영수님 단골집 (5회 방문)
✅ 1층, 계단 없음
✅ 큰 소리로 설명

     [예약하기]
     (큰 버튼)

━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 💡 고급 기능

### 1. 음성 검색
```
시니어: "근처에 싸고 맛있는 식당 알려줘"

AI 음성 응답:
"200미터 거리에 있는 '할매 순대국'을
추천드려요. 영수님이 지난번에 가셔서
맛있다고 하신 곳이에요. 가격은 7천원이고,
걸어서 3분 거리입니다. 예약할까요?"
```

### 2. 가족 추천 기능
```
가족 앱에서:
"아버지께 청소 업체 추천해주기"

→ AI가 아버지 프로필 분석
→ 3개 매장 추천
→ 가족이 선택
→ 아버지 앱에 "딸이 추천한 청소 업체" 알림
```

### 3. 단골 매장 자동 재예약
```
AI 감지:
"영수님, 지난달에도 이맘때 이발하셨네요.
이번에도 명성이발관 예약할까요?"

[예, 예약해줘] (원클릭)
```

---

## 🚀 로드맵

### Phase 1: 기본 추천 (런칭 시)
- 거리, 평점, 가격 기반 추천
- 접근성 필터링 (1층, 경사로 등)
- 과거 방문 이력 반영

### Phase 2: 개인화 강화 (3개월 후)
- 건강 상태 상세 반영
- 날씨, 시간 실시간 조정
- 가족 선호 학습

### Phase 3: AI 고도화 (6개월 후)
- 음성 검색 및 대화형 추천
- 단골 매장 자동 재예약
- 예측 추천 (다음에 필요한 서비스 예상)

---

## 📈 성공 지표

### 정량 지표
- 1순위 추천 클릭률: 60% 이상
- 추천 매장 예약률: 40% 이상
- 추천 매장 평균 평점: 4.5 이상
- 재방문율: 30% 이상

### 정성 지표
- 시니어 만족도: "내게 딱 맞는 매장을 찾아줘서 좋다"
- 가족 만족도: "부모님께 안전한 매장을 추천해줘서 안심"
- 매장 만족도: "우리 매장과 잘 맞는 고객이 와서 좋다"
